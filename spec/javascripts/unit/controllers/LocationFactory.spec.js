describe('LocationFactory', function() {

  beforeEach(module('brimApp'));

  var LocationFactory, httpBackend;
  var LocationData = [];

  beforeEach(inject(function(LocationFactory, $httpBackend) {
    httpBackend = $httpBackend
  }));
});